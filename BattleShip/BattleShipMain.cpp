// BattleShip.cpp : 콘솔 응용 프로그램에 대한 진입점을 정의합니다.
//

//class는 어떤 것을 소유할 것인지 정한다

//player 
//맵을 가지다(누구의 맵이라고 명시하지 않더라도 player가 가지고 있으면 그놈꺼라고 생각하겠지)
// hp가 있다(속성), Map을 가지다(소유) = has 소유>>속성
// 

//map
//사물은 소유를 증명할 수 없다. map has player(x) :: 경우에 따라서 가능할 때도

#include "stdafx.h"


int _tmain(int argc, _TCHAR* argv[])
{
	return 0;
}

